
import { useNavigate } from 'react-router-dom';
  // Make sure the path is correct

const AddProjectWithNavigate = (props) => {
  const navigate = useNavigate();
  return navigate;
};

export default AddProjectWithNavigate;
